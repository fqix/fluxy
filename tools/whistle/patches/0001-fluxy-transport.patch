--- a/lib/https/index.js
+++ b/lib/https/index.js
@@ -1055,8 +1055,19 @@
   client.on('error', handleAbort);
   client.on('response', function (svrRes) {
     svrRes.on('error', handleAbort);
+    // HTTP/1 cannot retain HTTP/2's trailers-only END_STREAM flag. Promote
+    // initial gRPC status fields to final trailers on the downstream H2 hop.
+    var grpcTrailers = {};
+    if (isH2 && svrRes.headers['grpc-status'] != null) {
+      Object.keys(svrRes.headers).filter(function (name) { return !/^(content-type|content-length|date|server|connection|transfer-encoding|trailer)$/.test(name); }).forEach(function (name) {
+        if (svrRes.headers[name] != null) {
+          grpcTrailers[name] = svrRes.headers[name];
+          delete svrRes.headers[name];
+        }
+      });
+    }
     svrRes.once('end', function () {
-      var trailers = svrRes.trailers;
+      var trailers = Object.assign({}, grpcTrailers, svrRes.trailers);
       if (!util.isEmptyObject(trailers)) {
         var rawHeaderNames = svrRes.rawTrailers
           ? getRawHeaderNames(svrRes.rawTrailers)
--- a/lib/https/load-cert.js
+++ b/lib/https/load-cert.js
@@ -10,6 +10,17 @@
 
 module.exports = function (socket, callback) {
   var servername = socket.servername;
+  var provider = require('../config').fluxyCertificate;
+  if (provider && !socket._fluxyCertificateChecked) {
+    socket._fluxyCertificateChecked = true;
+    return provider(servername).then(function (cert) {
+      if (cert) {
+        remoteCerts.set(servername, cert);
+        return callback(cert);
+      }
+      module.exports(socket, callback);
+    }, function () { callback(false); });
+  }
   var curCert = remoteCerts.get(servername);
   var plugin = rules.resolveSNICallback(socket);
   if (plugin) {
--- a/lib/inspectors/res.js
+++ b/lib/inspectors/res.js
@@ -205,6 +205,12 @@
       var originDomain = options.hostname;
       var now = Date.now();
       rules.getClientCert(req, function (key, cert, isPfx, cacheKey) {
+        if (req._fluxyTls) {
+          key = req._fluxyTls.key || key;
+          cert = req._fluxyTls.cert || cert;
+          options.ca = req._fluxyTls.ca;
+          if (cert) cacheKey = require('crypto').createHash('sha256').update(cert).digest('hex');
+        }
         rules.getProxy(
           options.href,
           options.isPlugin ? null : req,
--- a/lib/https/h2.js
+++ b/lib/https/h2.js
@@ -108,6 +108,7 @@
 }
 
 function addCert(opts, options, disabled) {
+  if (options.ca) opts.ca = options.ca;
   if (options.cert) {
     opts.key = options.key;
     opts.cert = options.cert;
